
var img = new SimpleImage ("chapel.png")

for (var pixel of img.values()) {
    if (pixel.getX() <= 77) {
        pixel.setRed(255);
    }
    if (pixel.getX() > 77 && pixel.getX() < 154) {
        pixel.setGreen(255);
    }
    else {
        pixel.setBlue(255);
    }
}
//print(img);
